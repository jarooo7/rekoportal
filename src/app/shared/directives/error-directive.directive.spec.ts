import { ErrorDirectiveDirective } from './error-directive.directive';

describe('ErrorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
